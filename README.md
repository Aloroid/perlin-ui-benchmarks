# perlin-ui-benchmarks
Basic benchmarks that update a grid of frames with a perlin noise generator.

Changing the benchmark can be done in src/client/init.client.luau and changing the ui_component variable.